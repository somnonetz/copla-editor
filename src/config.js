const autologin = false;

const host = 'https://xnat.tld/xnat/REST';

const credentials = {
  username: 'admin',
  password: 'password',
};

const defaultProject = 'project';
const defaultSubject = 'subject';

export { autologin, host, credentials, defaultProject, defaultSubject };
